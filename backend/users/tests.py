from django.test import TestCase

# TODO: Write tests for:
#   - User registration creates User + Farmer profile atomically
#   - Login is blocked if account_status != APPROVED
#   - JWT token contains role and email claims
#   - Role assignment (FARMER, MAO, SIBAT, ADMIN, AUCTION, SLAUGHTERHOUSESTAFF)
#   - UserDocument verification workflow (PENDING → APPROVED/REJECTED)
